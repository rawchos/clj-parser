#!/usr/bin/env bash

die () {
  echo >&2 "$@"
  exit 1
}

[ "$#" -eq 1 ] && [ -s $1 ] ||
  die "You must pass in a file with a size greater than 0 in order to parse records"

clj -M -m clj-parser.file-parser $1